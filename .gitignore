venv/
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
db.sqlite3
# Local snapshots taken before a destructive management command. Same client
# evidence as db.sqlite3, so they stay out of the repo the same way — the
# pattern above only matches the exact filename.
db.sqlite3.bak*
.env
.DS_Store
cookies.txt
*.json
*.log
_tmp_runserver.log
runserver.log
new_info.txt
apis/*.pdf
media/
*.backup
test video.png

staticfiles/

# Client meeting documents (shared privately - never commit to the public repo)
*.docx
